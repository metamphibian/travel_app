<p id="notice"><%= notice %></p>

<h1><%= "#{@destination.city}, #{@destination.country}" %></h1> 
<h5>Population Situation <%= number_with_delimiter @destination.population %></h5>  

<p>
  <%= @destination.description %>
</p>

<div class="btn-group">
	<%= link_to 'Edit', edit_destination_path(@destination), class: "btn btn-warning" %>
	<%= link_to 'Back', destinations_path, class: "btn btn-success" %>
</div>